export function computeFpCost(/*state, packsResult*/) { return 0; }
export function canSpend(/*actor, totalCost*/) { return { ok: true, have: 0, need: 0 }; }
export async function spend(/*actor, totalCost*/) { return { ok: true }; }
